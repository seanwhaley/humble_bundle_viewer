# Frequently Asked Questions

## Where do downloads go?

Files go to `download.base_folder` in `backend/config.yaml`.

## Do I need to keep the browser open?

No. `Playwright` runs the browser for the capture and closes it after.

## Can I limit downloads to e-book or audio book?

Yes. Set `platforms` in `backend/config.yaml`.
