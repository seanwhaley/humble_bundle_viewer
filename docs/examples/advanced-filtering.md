# Advanced Filtering

Use the captured data file to build custom filters.

## Ideas

- Select products where `category` equals `audio`.
- Keep downloads where `platform` equals `ebook`.
- Filter out files above a size threshold using `size_bytes`.

## Tip

Save a filtered copy of `library_products.json` before a new run.
